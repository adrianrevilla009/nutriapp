{
  "title": "Fasting window reminder",
  "body": "Your fasting window ({{ source_aggregate_id }}) is still open -- tap to log or end it.",
  "data": {"category": "{{ category }}", "source_aggregate_id": "{{ source_aggregate_id }}"}
}

