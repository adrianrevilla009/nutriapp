apiVersion: v2
name: activity-service
description: NutriApp activity-service -- manual exercise logging (event-driven CRUD). Wearable provider sync is interface-only in this MVP, see README.md.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"
