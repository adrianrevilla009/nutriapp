apiVersion: v2
name: frontend
description: NutriApp frontend — containerized Next.js app serving journey 1 (register -> log a food item -> see totals). See /plans/frontend/implementation-plan.md.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"
