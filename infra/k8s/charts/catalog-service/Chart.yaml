apiVersion: v2
name: catalog-service
description: NutriApp catalog-service — supermarket product inventory ingestion, normalization, and search.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"
