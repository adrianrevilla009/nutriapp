# infra/k8s/charts/catalog-service/ci/synthetic-values.yaml
#
# Checked-in fixture used ONLY by .github/workflows/catalog-service-ci.yml's
# helm-lint-and-template job to fill the fields Terraform normally injects
# at deploy time (catalog-service.tf) — ARNs/hosts/roles below are
# syntactically valid but point at nothing real. Never use these values
# outside CI (same pattern/rationale as identity-service's own
# ci/synthetic-values.yaml).

image:
  repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/catalog-service"
  tag: "ci-fixture"

serviceAccount:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-catalog-service-app-secrets"

networkPolicy:
  enabled: true
  ingressRules:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: kong
      ports:
        - protocol: TCP
          port: 8000

secretsManager:
  dbCredentials: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/catalog-service/db-credentials-XyZ123"
  usdaFdcApiKey: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/catalog-service/usda-fdc-api-key-AbCdEf"

dbProvision:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-catalog-service-db-provision"
  rdsHost: "nutriapp-dev-postgres.abc123.us-east-1.rds.amazonaws.com"
  rdsMasterSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:rds!db-abc123"
  dbCredentialSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/catalog-service/db-credentials-XyZ123"
  image:
    repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/db-provision"
    tag: "ci-fixture"
