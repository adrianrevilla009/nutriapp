{{/*
Delegates to the shared library chart's Deployment template
(infra/k8s/charts/_lib/templates/_deployment.tpl), same convention as
identity-service/profile-service.
*/}}
{{ include "nutriapp-lib.deployment" . }}
