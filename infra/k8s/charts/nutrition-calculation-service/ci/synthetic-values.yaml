# infra/k8s/charts/nutrition-calculation-service/ci/synthetic-values.yaml
#
# Checked-in fixture used ONLY by
# .github/workflows/nutrition-calculation-service-ci.yml's
# helm-lint-and-template job to fill the fields Terraform normally injects
# at deploy time (nutrition-calculation-service.tf) -- ARNs/hosts/roles
# below are syntactically valid but point at nothing real. Never use these
# values outside CI (same pattern/rationale as catalog-service's own
# ci/synthetic-values.yaml).

image:
  repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/nutrition-calculation-service"
  tag: "ci-fixture"

serviceAccount:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-nutrition-calculation-service-app-secrets"

networkPolicy:
  enabled: true
  ingressRules:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: kong
      ports:
        - protocol: TCP
          port: 8000

profileRevealEgress:
  enabled: true
  profileServicePodSelector:
    matchLabels:
      app.kubernetes.io/name: profile-service
  profileServiceInternalPort: 8001
  rdsPort: 5432
  redisPort: 6379
  rabbitmqPort: 5672

secretsManager:
  dbCredentials: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/nutrition-calculation-service/db-credentials-XyZ123"
  profileRevealCredential: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/profile-service/internal-reveal-credential-nutrition-calculation-service-AbCdEf"

dbProvision:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-nutrition-calculation-service-db-provision"
  rdsHost: "nutriapp-dev-postgres.abc123.us-east-1.rds.amazonaws.com"
  rdsMasterSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:rds!db-abc123"
  dbCredentialSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/nutrition-calculation-service/db-credentials-XyZ123"
  image:
    repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/db-provision"
    tag: "ci-fixture"
