# infra/k8s/charts/notification-service/ci/synthetic-values.yaml
#
# Checked-in fixture used ONLY by
# .github/workflows/notification-service-ci.yml's helm-lint-and-template
# job to fill the fields Terraform normally injects at deploy time
# (notification-service.tf) -- ARNs/hosts/roles below are syntactically
# valid but point at nothing real. Never use these values outside CI
# (same pattern/rationale as every other service's own
# ci/synthetic-values.yaml).

image:
  repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/notification-service"
  tag: "ci-fixture"

serviceAccount:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-notification-service-app-secrets"

networkPolicy:
  enabled: true
  ingressRules:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: kong
      ports:
        - protocol: TCP
          port: 8000

identityRevealEgress:
  enabled: true
  identityServicePodSelector:
    matchLabels:
      app.kubernetes.io/name: identity-service
  identityServicePort: 8000
  rdsPort: 5432
  rabbitmqPort: 5672
  externalHttpsPort: 443

secretsManager:
  dbCredentials: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/notification-service/db-credentials-XyZ123"
  identityRevealCredential: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/identity-service/internal-reveal-credential-AbCdEf"
  sesEndpoint: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/notification-service/ses-endpoint-GhIjKl"

dbProvision:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-notification-service-db-provision"
  rdsHost: "nutriapp-dev-postgres.abc123.us-east-1.rds.amazonaws.com"
  rdsMasterSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:rds!db-abc123"
  dbCredentialSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/notification-service/db-credentials-XyZ123"
  image:
    repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/db-provision"
    tag: "ci-fixture"

