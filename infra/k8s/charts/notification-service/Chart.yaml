apiVersion: v2
name: notification-service
description: NutriApp notification-service — transactional email (SES) and push (SNS) delivery, triggered by events from other services.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"

