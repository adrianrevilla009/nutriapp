apiVersion: v2
name: social-service
description: NutriApp social-service -- one-way follow connections between users and a Pro-gated activity feed composed from followed users' published recipes (CLAUDE.md section 2.2).
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"
