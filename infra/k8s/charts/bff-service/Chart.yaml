apiVersion: v2
name: bff-service
description: NutriApp bff-service — frontend response aggregation only, orchestration never business logic (ADR-0008).
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: nutriapp-lib
    version: "0.1.0"
    repository: "file://../_lib"
