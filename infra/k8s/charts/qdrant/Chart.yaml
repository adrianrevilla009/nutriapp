apiVersion: v2
name: qdrant
description: >-
  Self-hosted Qdrant vector store, shared platform-level instance
  (nutrition-assistant-service implementation plan section 9 resolution
  3). Standalone chart -- does NOT depend on infra/k8s/charts/_lib
  (nutriapp-lib), which is tailored to this repo's own FastAPI app
  services (health/readiness probes, db-provision-job hook, etc.) and
  does not fit a third-party stateful database workload. First
  third-party/non-app-service chart in this repo.
type: application
version: 0.1.0
appVersion: "1.11.3"
