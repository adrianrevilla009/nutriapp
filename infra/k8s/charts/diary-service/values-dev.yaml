replicaCount: 1

resources:
  requests:
    cpu: 75m
    memory: 128Mi
  limits:
    cpu: 375m
    memory: 256Mi

hpa:
  enabled: false # single replica in dev, scale-to-zero handles cost

pdb:
  enabled: false # meaningless at replicaCount: 1

image:
  pullPolicy: Always
