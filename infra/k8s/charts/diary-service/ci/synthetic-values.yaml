# infra/k8s/charts/diary-service/ci/synthetic-values.yaml
#
# Checked-in fixture used ONLY by .github/workflows/diary-service-ci.yml's
# helm-lint-and-template job to fill the fields Terraform normally injects
# at deploy time (diary-service.tf) -- ARNs/hosts/roles below are
# syntactically valid but point at nothing real. Never use these values
# outside CI. Mirrors profile-service's own synthetic-values.yaml fixture,
# included from day one here (identity-service only added this in a
# post-review addendum -- no reason to repeat that gap).

image:
  repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/diary-service"
  tag: "ci-fixture"

serviceAccount:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-diary-service-app-secrets"

networkPolicy:
  enabled: true
  ingressRules:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: kong
      ports:
        - protocol: TCP
          port: 8000

secretsManager:
  dbCredentials: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/diary-service/db-credentials-XyZ123"

dbProvision:
  irsaRoleArn: "arn:aws:iam::123456789012:role/nutriapp-dev-diary-service-db-provision"
  rdsHost: "nutriapp-dev-postgres.abc123.us-east-1.rds.amazonaws.com"
  rdsMasterSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:rds!db-abc123"
  dbCredentialSecretArn: "arn:aws:secretsmanager:us-east-1:123456789012:secret:nutriapp/dev/diary-service/db-credentials-XyZ123"
  image:
    repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/nutriapp/db-provision"
    tag: "ci-fixture"
