{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nutriapp.internal/schemas/body_metric_recorded.v1.json",
  "title": "BodyMetricRecorded",
  "type": "object",
  "required": ["event_id", "aggregate_id", "event_type", "version", "occurred_at", "payload", "metadata"],
  "properties": {
    "event_id": { "type": "string", "format": "uuid" },
    "aggregate_id": { "type": "string", "format": "uuid" },
    "event_type": { "const": "BodyMetricRecorded" },
    "version": { "const": 1 },
    "occurred_at": { "type": "string", "format": "date-time" },
    "payload": {
      "type": "object",
      "required": ["user_id", "metric_type", "value", "recorded_at"],
      "properties": {
        "user_id": { "type": "string", "format": "uuid" },
        "metric_type": { "type": "string", "enum": ["height", "age", "sex", "activity_level"] },
        "value": { "type": "string", "description": "AES-256-GCM ciphertext (base64), per-user envelope-encrypted -- never the plaintext value." },
        "recorded_at": { "type": "string", "format": "date-time" }
      },
      "additionalProperties": false
    },
    "metadata": {
      "type": "object",
      "required": ["correlation_id"],
      "properties": {
        "correlation_id": { "type": "string" },
        "causation_id": { "type": ["string", "null"] },
        "user_id": { "type": ["string", "null"] }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
